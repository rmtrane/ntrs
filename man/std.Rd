% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/std.R
\name{std}
\alias{std}
\title{Standardize neuropsychological test scores}
\usage{
std(scores, ..., method = NULL, version = NULL)
}
\arguments{
\item{scores}{An \code{npsych_scores} object, such as \code{MOCATOTS(c(25, 28))}.}

\item{...}{Named covariates passed through to the underlying
\verb{std_using_*()} method (e.g., \code{age}, \code{sex}, \code{educ}).}

\item{method}{A single string naming the standardization method (e.g.,
\code{"norms"}, \code{"regression"}). When \code{NULL} (the default), the default
method registered via \code{\link[=set_std_defaults]{set_std_defaults()}} is used.}

\item{version}{A single string identifying the version of the method
(e.g., \code{"nacc"}, \code{"updated_2025.06"}). When \code{NULL} (the default), the
default version registered via \code{\link[=set_std_defaults]{set_std_defaults()}} is used. Ignored
when the resolved method does not use versioned data.}
}
\value{
An \link{std_npsych_scores} object (inherits \code{numeric}). Access
standardization metadata via S7 properties:
\describe{
\item{\verb{@method}}{The standardization method used (e.g., \code{"norms"},
\code{"regression"}).}
\item{\verb{@version}}{The version identifier (e.g., \code{"nacc"},
\code{"updated_2025.06"}), or \code{NULL} if the method is unversioned.}
\item{\verb{@description}}{A human-readable summary of the standardization
applied, including covariates.}
\item{\verb{@scores_subclass}}{The name of the originating \code{npsych_scores}
subclass (e.g., \code{"MOCATOTS"}).}
}
}
\description{
User-facing wrapper that dispatches to the appropriate \verb{std_using_*()}
method for an \code{npsych_scores} object. When \code{method} and \code{version} are
omitted, the registered defaults (see \code{\link[=set_std_defaults]{set_std_defaults()}}) are used.
}
\examples{
\dontrun{
# Using registered defaults
std(MOCATOTS(c(25, 28)), age = 72, sex = 1, educ = 16)

# Specifying method and version explicitly
std(MOCATOTS(c(25, 28)), method = "norms", version = "nacc",
    age = 72, sex = "m", educ = 16)
}

}
\seealso{
\code{\link[=std_data]{std_data()}} for batch-standardizing every \code{npsych_scores}
column in a data frame, \code{\link[=list_std_methods]{list_std_methods()}}, \code{\link[=list_method_versions]{list_method_versions()}},
\code{\link[=get_std_defaults]{get_std_defaults()}}, \code{\link[=set_std_defaults]{set_std_defaults()}}.
}
