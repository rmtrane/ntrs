% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/std_data.R
\name{std_data}
\alias{std_data}
\title{Standardize all \code{npsych_scores} columns in a data frame}
\usage{
std_data(
  data,
  ...,
  methods = list(),
  prefix_std = "z_",
  prefix_raw = NULL,
  .cols = NULL
)
}
\arguments{
\item{data}{A \code{data.frame} or \code{data.table} containing one or more
\code{npsych_scores} columns.}

\item{...}{Named covariates shared across all score columns (e.g.,
\code{age}, \code{sex}, \code{educ}). These are passed to every \code{\link[=std]{std()}} call and
must be supplied explicitly. Values can be bare column names
referencing columns in \code{data} (e.g., \code{age = age}) or explicit
vectors (e.g., \code{age = c(72, 65, 80)}).}

\item{methods}{An optional named list keyed by \code{npsych_scores} \strong{class
name} (not column name). Each element is a named character vector of
the form \code{c(method = "...", version = "...")}. Both elements are
optional within each entry; omitted elements resolve to the registered
default. Classes not listed use their defaults.

\if{html}{\out{<div class="sourceCode">}}\preformatted{methods = list(
  MOCATOTS = list(method = "regression", version = "nacc"),
  ANIMALS  = list(method = "norms")
)
}\if{html}{\out{</div>}}}

\item{prefix_std}{A single string prepended to each score column name to
form the new column name. Defaults to \code{"z_"}.}

\item{prefix_raw}{Optional; if character string, used as prefix for scores that
are standardized. This enables you to obtain pairs of scores, for example
\code{raw_MOCATOTS} and \code{z_MOCATOTS} for bookkeeping}

\item{.cols}{An optional character vector of \code{npsych_scores} \strong{column
names} to standardize. When \code{NULL} (the default), all \code{npsych_scores}
columns are processed.}
}
\value{
The input \code{data} with additional standardized score columns
appended. The original columns are left unchanged (unless \code{prefix_raw}
is specified, in which case they are renamed with that prefix). The
return type matches the input type (\code{data.frame} or \code{data.table}).

Each standardized column is an \link{std_npsych_scores} object with S7
properties \verb{@method}, \verb{@version}, \verb{@description}, and
\verb{@scores_subclass}. Use \code{\link[=methods_from_std_data]{methods_from_std_data()}} to extract the
method and version from all standardized columns at once.

The returned data frame carries two attributes:
\describe{
\item{\code{prefix_std}}{The prefix used for standardized columns (default
\code{"z_"}).}
\item{\code{prefix_raw}}{The prefix used for raw score columns, or \code{NULL}
if \code{prefix_raw} was not specified.}
}
}
\description{
Finds every column in \code{data} that inherits from \code{npsych_scores},
standardizes it via \code{\link[=std]{std()}}, and adds the result as a new column with a
\code{"z_"} prefix (e.g., \code{MOCATOTS} → \code{z_MOCATOTS}).

Each score class can use a different method/version combination.
Classes not mentioned in \code{methods} fall back to their registered
defaults (see \code{\link[=set_std_defaults]{set_std_defaults()}}).
}
\examples{
\dontrun{
# Bare column names — evaluated against data
std_data(my_data, age = age, sex = sex, educ = educ)

# Explicit vectors work too
std_data(my_data, age = my_data$age, sex = my_data$sex, educ = my_data$educ)

# Override method/version for specific test classes
std_data(
  my_data,
  methods = list(
    MOCATOTS = list(method = "norms", version = "nacc"),
    ANIMALS  = list(method = "regression", version = "updated_2025.06")
  ),
  age = age, sex = sex, educ = educ
)
}

}
\seealso{
\code{\link[=std]{std()}} for standardizing a single \code{npsych_scores} vector.
}
