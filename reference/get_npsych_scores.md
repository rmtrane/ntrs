# Get an npsych_scores constructor by name

Get an npsych_scores constructor by name

## Usage

``` r
get_npsych_scores(name)
```

## Arguments

- name:

  A single string, e.g. "REYTOTAL".

## Value

The S7 class object (which is callable as a constructor).
