# UDSBENTD Test Scores

Create a `UDSBENTD` object to hold UDSBENTD scores.

## Usage

``` r
UDSBENTD(x = numeric())
```

## Arguments

- x:

  Numeric scores.

## Value

An object of class `UDSBENTD`.
