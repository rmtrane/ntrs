# Is `x` an npsych_scores object?

\#' A short description...

## Usage

``` r
is_npsych_scores(x)
```

## Arguments

- x:

  An R object.

## Value

A single logical value: `TRUE` if `x` is an `npsych_scores` object,
`FALSE` otherwise.
