# DIGIFLEN Test Scores

Create a `DIGIFLEN` object to hold DIGIFLEN scores.

## Usage

``` r
DIGIFLEN(x = numeric())
```

## Arguments

- x:

  Numeric scores.

## Value

An object of class `DIGIFLEN`.
