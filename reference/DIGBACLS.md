# DIGBACLS Test Scores

Create a `DIGBACLS` object to hold DIGBACLS scores.

## Usage

``` r
DIGBACLS(x = numeric())
```

## Arguments

- x:

  Numeric scores.

## Value

An object of class `DIGBACLS`.
