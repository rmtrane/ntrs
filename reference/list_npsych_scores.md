# List `npsych_scores` subclasses

This function is a helper that lists all available subclasses, i.e. all
functions created using `new_class <- new_npsych_scores(...)`.

## Usage

``` r
list_npsych_scores()
```

## Value

A character vector of valid test subclass names.
