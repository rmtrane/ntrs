# Get standardization defaults `npsych_scores`

Get standardization defaults `npsych_scores`

## Usage

``` r
get_std_defaults(scores)
```

## Arguments

- scores:

  S7 object of class `npsych_scores` with subclass.

## Value

Character string with default method and version (when applicable)
