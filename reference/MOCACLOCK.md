# MOCACLOCK Test Scores

Create a `MOCACLOCK` object to hold MOCACLOCK scores.

## Usage

``` r
MOCACLOCK(x = numeric())
```

## Arguments

- x:

  Numeric scores.

## Value

An object of class `MOCACLOCK`.
